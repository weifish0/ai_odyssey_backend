services:
  - type: web
    name: AI Odyssey Backend
    runtime: python
    plan: starter
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    region: singapore
    startCommand: python init_database.py && python migrate_users.py && python main.py